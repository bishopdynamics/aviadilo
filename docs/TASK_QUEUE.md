# Task Queue

Agent-worked, ordered queue. Processing rules live in `AGENTS.md` and project-specific overrides in `PROJECT.md`.

## Queue

Current follow-up [in-progress]: optional default-enabled You are here icon under docs/spec/FEATURE_SPEC_reference_marker.md is implemented and locally verified (308 frontend/515 backend/24 browser tests, native HA admin/regular/read-only, actual HACS candidate install). Preparing v0.2.0-dev.4 publication; configured map anchor, HA home by default. User assessment follows.

Current bug repair delivered: non-admin kiosk asset subscription fixed in v0.2.0-dev.3 (6c95c0e). Parent 301 frontend/512 backend/23 browser gates, regular/read-only native loading/clear/reconnect, remote workflows, actual public ZIP and HACS installation passed. User confirmed on 2026-09-09 that the fix works great. Keep existing HA roles; no MQTT or repeated profile/cache reset is needed.

Current follow-up implemented and delivered: optional layer buttons and page auto height under docs/spec/FEATURE_SPEC_map_layout.md, commit 9d714aa, published v0.2.0-dev.2. Parent 282 frontend/489 backend/23 browser checks, native HA layouts/editor/120-second stability, all remote workflows, public ZIP verification and actual published-bytes HACS installation passed. Await user assessment; keep ROOT_SPEC in progress. User TODO remains untouched.

Continuation 2026-09-08 complete for the three requested implementation slices: slice 4 faefc77, slice 5 9ec36b8, slice 6 c2ddc49. Published prerelease v0.2.0-dev.1; actual public ZIP is byte-identical to the tested candidate and actual HACS upgrade using published metadata/bytes passed. Parent gates: 273 frontend + 486 backend + 20 Chromium tests; native upgrade, 20-minute saved/editor endurance, warm return/second-browser/restart and remote native/hassfest/HACS/release workflows passed. ROOT_SPEC remains [in-progress] for user kiosk assessment and the separate unanswered native-preview-remount choice; no stronger cross-remount guarantee is claimed. Normal v0.1.0 remains Latest.

1. [in-progress] Implement `docs/spec/ROOT_SPEC.md` — approved 2026-09-06; slice 1 started 2026-09-06.
   - **HACS update repair user-confirmed 2026-09-08:** normal v0.1.0 release from 84c9231 is GitHub Latest with verified aviadilo.zip. The user confirmed the update worked, map tile caching/loading is great and editing no longer shows placeholder data. Actual HACS default Update from dev.2 passed using published metadata/bytes; future main commits/prereleases do not trigger false normal-channel updates. No repeat download workaround is needed. This fixes distribution ahead of kiosk slice 6; the preview-remount decision remains pending.
   - **Kiosk work:** implement `docs/spec/FEATURE_SPEC_kiosk_refinement.md` — detailed spec approved by the user on 2026-09-08; slices 1 (asset contracts) and 2 (integration basemap cache and external photos) implemented and independently verified. Slice 3, one frontend pipeline and fast asset rendering: implementation and automated/native checks are verified; the user confirmed caching/loading and real edit-mode data, with a user decision pending on HA-forced preview element replacement and cross-remount client continuity. Product direction is approved: shared basemap cache; exactly the same real pipeline in editing/live/picker views; quiet card retaining layer buttons/Recenter/optional aircraft list; map themes; one wind display mode and color. All six implementation slices now have verified code, and v0.2.0-dev.1 packages the kiosk changes. This remains under the ROOT_SPEC umbrella pending user assessment and the separate lifecycle decision.
   - **Earlier session wrap requested 2026-09-08:** no new slice started at that checkpoint. The then-remaining order was: slice 4 card-v2/theme/wind modes and color; slice 5 quiet card and read-only inspection; slice 6 packaged acceptance. Existing extra info is the planned slice 5 work, not a newly discovered bug. Keep the separate preview-remount question recorded.
   - Kiosk refinement slice 3: parent full checks passed 201 frontend + 463 backend + 15 Chromium scenarios. Native warm 16-tile return 23–40 ms, second browser 37–82 ms, post-HA-restart 35.4 ms; zero extra upstream calls. See docs/research/ha-preview-lifecycle.md for the pending platform-lifecycle decision. No release/version change.
   - Kiosk refinement slice 2 verification: 194 frontend + 462 backend + 11 Chromium tests passed; lint/types/mypy on 44 files, builds and 28-file local ZIP passed. Independent actual HA HTTP/WS acceptance verified 16 warm tiles for two users in 10 ms, disk reuse after reload in 8 ms, zero repeated upstream requests during cooldown, and clear generation/event/old-request rejection. Renderer activation and native preview equivalence remain slice 3; no release/version change.
   - Kiosk refinement slice 1 verification: 194 frontend + 332 backend + 11 offline Chromium tests passed; lint/types/mypy on 38 files, builds and 25-file local ZIP passed. New asset contracts/coordinator/opt-in backend stubs are ready; no production activation or release. See docs/development.md and the feature spec.
   - Slice 1 implemented and independently verified: Node 24.20.0/Python 3.14.2 setup, v1 schemas/fixtures, offline card stub, HACS layout and deterministic ZIP, pinned CI/release tooling. `make check`: 31 frontend + 32 backend tests passed; Chromium 151.0.7922.34 verified dev, built fixture and extracted packaged module with zero provider requests.
   - First-run setup is complete; `docs/FIRST_RUN.md` removed. Repository/hook trust, identity, stack, GitHub connection and initial upstream push are settled.
   - Slice 2 implemented and independently verified 2026-09-07: integration setup/options, shared scheduler/cache, safe diagnostics and versioned module bootstrap. `make check`: 31 frontend + 97 backend tests passed. Real HA 2026.9.1/Chromium setup, options reload, cache clear, diagnostics and final-ZIP restart/module rendering passed.
   - Slice 3 implemented and independently verified 2026-09-07: authenticated WS/HTTP, captured publications and bounded tile gateway, frontend client lifecycle/reconnect. `make check`: 57 frontend + 169 backend tests passed. Real HA/Chromium client handshake, revisions, connection ownership, hide/dispose, reload and final-ZIP restart passed.
   - Slice 4 implemented and independently verified 2026-09-07: Leaflet map, all-settings graphical editor, device_tracker people/radius filtering, central viewport and offline previews. `make check`: 83 frontend + 169 backend tests passed. HA/Chromium editor save/reopen, tracker return/manual view, picker isolation and final-ZIP restart passed; physical touch remains unclaimed.
   - Slice 5 implemented and independently verified 2026-09-07 after explicit continuation: adsb.fi/ADSB.lol adapters, shared collection/cache/status, exact radius and reusable aircraft map/list/editor components. `make check`: 91 frontend + 195 backend tests passed. Standalone Chromium selection/viewport/freshness/editor/cleanup checks passed; two bounded ocean-location provider probes returned valid empty data and no-store. Final card wiring remains slice 8.
   - Slice 6 implemented and independently verified 2026-09-07: RainViewer/MRMS/KSOX metadata and tile adapters, shared pacing/cache, bounded playback/legends/editor, and optional per-caller client cancellation. `make check`: 109 frontend + 229 backend tests passed. Standalone Chromium cached/slow playback, paused refresh, native overzoom, marker interaction, error/coverage, sizing and cleanup passed. One metadata/overview request per source returned HTTP200; card composition remains slice 8.
   - Slice 7 implemented and independently verified 2026-09-07: DWD ICON-global WCS, shared 12-region collection/cache, actual scaled geometry/dateline bands, arrows/barbs/particles and graphical controls. `make check`: 120 frontend + 245 backend tests passed. Standalone Chromium direction, limits, simulated reduced motion/visibility, no-data, controls and cleanup passed; three bounded DWD metadata/grid probes succeeded. Card composition remains slice 8.
   - Slice 8 implementation and local acceptance 2026-09-07: all four layers composed, lifecycle/revision/reconnect recovery, synthetic previews, natural sections sizing, complete visual editor, offline Playwright and isolated HA tooling. Root CI gates passed 123 frontend + 258 backend + 11 Chromium tests. HA 2026.9.1 graphical setup/options, native picker/editor save/reopen, packaged dev.1→dev.2 upgrade, retained settings and warm-cache restart passed. HACS 2.0.5's actual installer passed with mocked GitHub transport; pinned hassfest passed. A 20-minute two-client real-HA playback/animation soak passed with zero JavaScript/tile errors and bounded resources. `.bishop/` is committed with project changes per the user's correction.
   - 2026-09-08 delivery follow-up fixed/published: `d68a1b2`, tag `v0.1.0-dev.2`. Published ZIP downloaded with HTTP 200 and matching checksum, validated and installed into an isolated config. Explicit version-tag workflow now publishes the checked prerelease; MIT chosen by user and shipped in ZIP. All prior runtime bytes unchanged. Remote release/native CI/hassfest passed; HACS 8/9 checks pass, with only GitHub topics still missing. User asked to add topics. User confirmed HACS download succeeds when explicitly selecting v0.1.0-dev.2 under Need a different version?; the prerelease-only default can otherwise select the latest commit. User subsequently confirmed the integration installed without issues on their HA instance. User added the card and confirmed Done exits synthetic dashboard editing to apparent real data. Saved-card acceptance checklist is in docs/user-guide.md; detailed live-layer behavior and physical tablet acceptance remain pending. Keep ROOT_SPEC in progress. See `docs/development.md`.
   - Use the approved eight serial slices, template-based worker briefs, explicit model/effort, and post-integration verification/review. Commit each slice and update status; follow the normal slice handoff cadence.
   - HACS is required from the first release. Remote: `github`; upstream: `github/main`; repository: `bishopdynamics/aviadilo`.

## Completed

- 2026-09-06 — Initial Planning completed by user approval of the revised `ROOT_SPEC.md`. Combined-map research and the Claremont source-comparison spike are preserved. Selected radar: RainViewer (default), NOAA MRMS, NOAA KSOX; wind: DWD ICON global.

## Repository maintenance

- 2026-09-08 — Investigated repeated Home Assistant/HACS validation failure. Latest inspected run 34270035858: hassfest passed, HACS failed only missing topics (8/9 passed). User has now saved `hacs` and `home-assistant`; GitHub API confirms them along with description and MIT. Validation run [34276525393](https://github.com/bishopdynamics/aviadilo/actions/runs/34276525393) on spec commit dcf57cf passed after the metadata correction. No runtime/workflow weakening or feature spec was needed for this fix.

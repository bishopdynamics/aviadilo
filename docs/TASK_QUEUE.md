# Task Queue

Agent-worked, ordered queue. Processing rules live in `AGENTS.md` and project-specific overrides in `PROJECT.md`.

## Queue

1. [in-progress] Implement `docs/spec/ROOT_SPEC.md` — approved 2026-09-06; slice 1 started 2026-09-06.
   - Slice 1 implemented and independently verified: Node 24.20.0/Python 3.14.2 setup, v1 schemas/fixtures, offline card stub, HACS layout and deterministic ZIP, pinned CI/release tooling. `make check`: 31 frontend + 32 backend tests passed; Chromium 151.0.7922.34 verified dev, built fixture and extracted packaged module with zero provider requests.
   - First-run setup is complete; `docs/FIRST_RUN.md` removed. Repository/hook trust, identity, stack, GitHub connection and initial upstream push are settled.
   - Slice 2 implemented and independently verified 2026-09-07: integration setup/options, shared scheduler/cache, safe diagnostics and versioned module bootstrap. `make check`: 31 frontend + 97 backend tests passed. Real HA 2026.9.1/Chromium setup, options reload, cache clear, diagnostics and final-ZIP restart/module rendering passed.
   - Slice 3 implemented and independently verified 2026-09-07: authenticated WS/HTTP, captured publications and bounded tile gateway, frontend client lifecycle/reconnect. `make check`: 57 frontend + 169 backend tests passed. Real HA/Chromium client handshake, revisions, connection ownership, hide/dispose, reload and final-ZIP restart passed.
   - Slice 4 implemented and independently verified 2026-09-07: Leaflet map, all-settings graphical editor, device_tracker people/radius filtering, central viewport and offline previews. `make check`: 83 frontend + 169 backend tests passed. HA/Chromium editor save/reopen, tracker return/manual view, picker isolation and final-ZIP restart passed; physical touch remains unclaimed.
   - Slice 5 implemented and independently verified 2026-09-07 after explicit continuation: adsb.fi/ADSB.lol adapters, shared collection/cache/status, exact radius and reusable aircraft map/list/editor components. `make check`: 91 frontend + 195 backend tests passed. Standalone Chromium selection/viewport/freshness/editor/cleanup checks passed; two bounded ocean-location provider probes returned valid empty data and no-store. Final card wiring remains slice 8.
   - Slice 6 implemented and independently verified 2026-09-07: RainViewer/MRMS/KSOX metadata and tile adapters, shared pacing/cache, bounded playback/legends/editor, and optional per-caller client cancellation. `make check`: 109 frontend + 229 backend tests passed. Standalone Chromium cached/slow playback, paused refresh, native overzoom, marker interaction, error/coverage, sizing and cleanup passed. One metadata/overview request per source returned HTTP200; card composition remains slice 8.
   - Slice 7 implemented and independently verified 2026-09-07: DWD ICON-global WCS, shared 12-region collection/cache, actual scaled geometry/dateline bands, arrows/barbs/particles and graphical controls. `make check`: 120 frontend + 245 backend tests passed. Standalone Chromium direction, limits, simulated reduced motion/visibility, no-data, controls and cleanup passed; three bounded DWD metadata/grid probes succeeded. Card composition remains slice 8.
   - Slice 8 implementation and local acceptance 2026-09-07: all four layers composed, lifecycle/revision/reconnect recovery, synthetic previews, natural sections sizing, complete visual editor, offline Playwright and isolated HA tooling. Root CI gates passed 123 frontend + 258 backend + 11 Chromium tests. HA 2026.9.1 graphical setup/options, native picker/editor save/reopen, packaged dev.1→dev.2 upgrade, retained settings and warm-cache restart passed. HACS 2.0.5's actual installer passed with mocked GitHub transport; pinned hassfest passed. A 20-minute two-client real-HA playback/animation soak passed with zero JavaScript/tile errors and bounded resources. `.bishop/` is committed with project changes per the user's correction.
   - 2026-09-08 delivery follow-up fixed/published: `d68a1b2`, tag `v0.1.0-dev.2`. Published ZIP downloaded with HTTP 200 and matching checksum, validated and installed into an isolated config. Explicit version-tag workflow now publishes the checked prerelease; MIT chosen by user and shipped in ZIP. All prior runtime bytes unchanged. Remote release/native CI/hassfest passed; HACS 8/9 checks pass, with only GitHub topics still missing. User asked to add topics. User confirmed HACS download succeeds when explicitly selecting v0.1.0-dev.2 under Need a different version?; the prerelease-only default can otherwise select the latest commit. Integration/dashboard/physical tablet acceptance remain pending; keep ROOT_SPEC in progress. See `docs/development.md`.
   - Use the approved eight serial slices, template-based worker briefs, explicit model/effort, and post-integration verification/review. Commit each slice and update status; follow the normal slice handoff cadence.
   - HACS is required from the first release. Remote: `github`; upstream: `github/main`; repository: `bishopdynamics/aviadilo`.

## Completed

- 2026-09-06 — Initial Planning completed by user approval of the revised `ROOT_SPEC.md`. Combined-map research and the Claremont source-comparison spike are preserved. Selected radar: RainViewer (default), NOAA MRMS, NOAA KSOX; wind: DWD ICON global.

# Handoff Archive 17

- **2026-09-08 — Kiosk follow-up specified, implementation not started.** Draft `docs/spec/FEATURE_SPEC_kiosk_refinement.md` uses the template and is linked as a ROOT_SPEC addendum. User approved the product direction and explicitly requires the same real pipeline in saved/edit/picker contexts, including cache misses; any proposed exception must be discussed first. Layer buttons, Recenter, optional aircraft list and selection popups remain.
- Draft covers integration-owned OSM cache with fast local hits/90-day bounded retention respecting freshness; external-avatar routing; no synthetic production branch; quiet healthy card and error-only inspection; theme; single wind mode/color; v1→v2 migration; six serial slices with owned files and verification. Engineering choices are proposed by the draft, not implemented. Review this spec before coding; do not reopen settled direction.
- PROJECT.md records the new production-pipeline rule. Current published dev.2 still has synthetic editing and direct OSM fetching, so earlier implementation notes describe what this feature replaces. Dev/test fixtures remain offline at network boundaries. ROOT_SPEC is the umbrella task; next work is spec review, then its slices after implementation approval.
- User also reported repeated GitHub validation failures. Latest inspected run 34270035858 (702e8ed) passed hassfest and 8/9 HACS checks, failing only topics. User has now saved hacs/home-assistant; GitHub API confirms both along with description and MIT. No application/workflow bug or reason to disable checks was found. **Verified fixed:** [validation run 34276525393](https://github.com/bishopdynamics/aviadilo/actions/runs/34276525393) on dcf57cf completed successfully.
- User-authored `docs/TODO.md` notes remain modified and uncommitted; agent did not alter or mark them done. `.bishop/` remains version-controlled and must be included if it changes. No implementation code, release, tag, HA config or provider data was changed in this planning task.
- Resource audit: no new workers/servers; only main worktree and no stale worker trees. Metadata investigation evidence `/tmp/aviadilo-validation-latest.json`. Prior public prerelease/installation and local HA acceptance details are in archive 16 and docs/development.md.

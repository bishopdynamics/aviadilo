# Handoff Main Index

This is where a new session looks for handoff information from previous sessions. Keep this file minimal: it holds the live current-session block; when it grows too large, split older entries into `archive_<number>.md` next to this file and reference them below. Durable always-read info belongs in `evergreen.md` instead.

## Current

- 2026-09-01 (evening) — Template v2: Claude Code can now dispatch Codex workers. Researched the Codex 0.152.0 CLI, smoke-tested `codex exec -C` in a linked worktree (no trust prompt, `AGENTS.md` loaded, `workspace-write` writes fine, the project's `.codex/hooks.json` rm-guard hook enforced), then added template-owned `dispatch-codex.sh` (worktree `.agent-worktrees/<slice>`, branch `codex/<slice>`, defaults Sol/high/workspace-write, artifacts under `.agent-worktrees/_runs/<slice>/`, `--remove` for cleanup), the dispatch paragraph in `AGENTS.md`, a mandatory worker preamble in `TASK_BRIEF_TEMPLATE.md`, the `.agent-worktrees/` ignore rule, README section, and `docs/spec/FEATURE_SPEC_codex_dispatch.md`. Bumped `TEMPLATE_VERSION` to 2, ran the migration (40 projects updated, tool reports all current), and committed it in each project (`chore: apply project-template-a v2 (Codex dispatch helper)`). The first real dispatch on a genuine slice is still to come; see the spec's deferred item.

- 2026-09-05 (later) — **Template v5: `dev.sh` launches Codex with `gpt-6-astra`.** User decision; the 1,000,000-token context and 900,000 compaction overrides stay (Astra and Sol share `context_window` 272k / `max_context_window` 872k in `~/.codex/models_cache.json`). README launcher sentence updated. Migration applied and committed in 49 projects (`chore: apply project-template-a v5 (dev.sh launches Astra)`); `simpler-camera-card` ignores `dev.sh` in its own `.gitignore`, so only its marker was committed there. Tool reports nothing to update. Lesson for the per-project commit script: check `git ls-files` before `git add` and handle errors per project instead of aborting the loop.

- 2026-09-05 — **Template v4: Astra/Sol worker tiers.** The user asked to use Astra (Codex id `gpt-6-astra`, a Fable-class model, efforts up to `ultra`) for complex tasks and Sol for less demanding ones. `AGENTS.md` Codex tiers are now `gpt-6-astra`/`high` demanding, `gpt-5.6-sol`/`medium` lighter or read-heavy, `gpt-5.6-luna`/`low` narrow repetitive (Terra dropped from the mapping). hanuman's own dispatch default was already `gpt-6-astra` (its commit `069607d`). Version 4, changelog entry, migration applied and committed in 49 projects (`chore: apply project-template-a v4 (Astra/Sol worker tiers)`), tool reports nothing to update. Not changed: `dev.sh` still launches interactive Codex sessions with `gpt-5.6-sol` and the 1,000,000-token context overrides; whether the orchestrator session itself should move to Astra is the user's call.

- 2026-09-03 — **Template v3: Codex workers through hanuman.** Reviewed the v2 Codex guidance against the finished `hanuman` MCP server (`/mnt/Fast/projects/hanuman`, registered at user scope, eight `mcp__hanuman__*` tools) and wrote `docs/spec/FEATURE_SPEC_hanuman_dispatch.md`; the user decided: retire `dispatch-codex.sh`, Codex is the default worker platform with Claude as the fallback when Codex is out of usage (hanuman's `status` will report usage and time to reset), and first run gets a Codex trust step. Probed Codex trust: project trust is per absolute path and hook trust per hook-file path plus content hash in `~/.codex/config.toml`; an untrusted repository never loads its `.codex/hooks.json`, but once the repository and its hook are trusted the `rm` guard fires inside linked worktrees too (the user ran the decisive probe). Implemented as the first real hanuman dispatches from a template project: two `gpt-5.6-sol`/`high` workers in parallel worktrees (`v3-guidance`: `AGENTS.md`, brief template, `FIRST_RUN.md`, `.gitignore` comment, README, `new-project.sh`, changelog, version 3, deleted the script; `v3-retired-files`: `RETIRED_FILES` in `migrate-project-docs.py` plus two tests), both merged (`8105094`), 27 tests green. Ran the migration: 44 projects updated 2→3 and `prometheus` (an old-layout project that had appeared since the last sweep) converted, then committed in each (`chore: apply project-template-a v3 (hanuman dispatch)`; prometheus also got its carried-over rules folded into `PROJECT.md`). Tool reports nothing to update. Hanuman ergonomics: `wait` and `approve` worked as designed; every `apply_patch` and every Git index write in a linked worktree raised an approval (the index lives under the main repo's `.git`, outside the sandbox), `accept_for_session` covered later Git commands but not later file changes, so a slice with many edits means several approvals; the structured reports were accurate, and one worker correctly flagged a brief error of mine (`sh -n dev.sh`; `dev.sh` is Bash). Briefs are in this session's scratchpad and in each run directory under `.agent-worktrees/_runs/`.

### Next session

1. Codex trust bookkeeping: only 14 template projects have project trust and 4 have hook trust (`beamvm`, `bishop-terminal`, `project-template-a`, `regilo`). Until a project is trusted, Codex workers there run without the `rm` guard; the user trusts each by running `codex` once in the project root and accepting the hook prompt. The full list is in Elefant (tags `project-template-a, codex, trust`).
2. hanuman follow-ups observed here (record in the hanuman project when working there): `accept_for_session` not covering subsequent file-change approvals; consider a dispatch option or default that pre-approves in-worktree patches and Git index writes; usage and time-to-reset in `status` (already planned).
3. When a template-owned file changes again, bump `TEMPLATE_VERSION`, add a changelog entry, and run `./migrate-project-docs.sh /mnt/Fast/projects`.

## Archives

- `archive_1.md` — migration tool rebuild, ownership redesign and rollout, template v1 conversion and tidy-up (2026-08-05 to 2026-09-01).

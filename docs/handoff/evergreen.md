# Evergreen Handoff

Durable handoff info — the kind of stuff that should be read at the start of every session, regardless of what the last session did. Keep entries current; delete them when they stop being true.

## Evergreen Entries

- Project: Aviadilo (`aviadilo`), one larger Lovelace kiosk map combining optional aircraft, radar, wind, and household locations. Original idea: `docs/idea/initial-idea.md`; current user decisions: `docs/idea/revised-direction.md`.
- Full `docs/spec/ROOT_SPEC.md` approved by the user on 2026-09-06. Implementation began with slice 1 that day. All documented engineering defaults and deferrals are accepted; do not request repeated approval.
- HACS is required from the first release (user correction). Public GitHub repository: `bishopdynamics/aviadilo`; remote `github`, upstream `github/main`. User committed/pushed existing work as `0a91da2`. Keep this remote name; no GitLab repository or `origin` is needed.
- Compatibility: user runs latest HA on a Chromium tablet-PC kiosk. Official latest release verified as HA 2026.9.1 on 2026-09-06; use that test baseline and record the tested Chromium build. No more broad kiosk/browser clarification is needed.
- The user currently uses HACS `weather-radar-card` and values its wind markers/animations. Include optional static and animated wind in the design.
- Target region: California, North America. Weather choices selected after spike review (2026-09-06): RainViewer (default), NOAA MRMS, and NOAA KSOX for radar; DWD ICON global only for wind. The spike confirms the DWD global coverage identity; use it rather than the old card's ICON-D2 label.
- Household locations use `device_tracker` entities. Support them directly. A configurable people-radius filter must exclude far-away travellers from drawing and map fitting; no conversion to person entities is required.
- All supported configuration needs a visual editor, including advanced settings.
- Public data access must remain free for household use. Respect half of documented provider allowances across the installation, including tile/grid requests and retries; cache and share work. Approved aircraft sources: adsb.fi default, ADSB.lol alternative. Approved cache budgets: 512 MiB disk, 64 MiB backend memory.
- Source-quality research: `docs/research/weather-source-quality.md`. Earlier suggestions to add NDFD/HRRR wind or ECCC radar were superseded by the user's selection. Those sources and other candidates remain research only, outside implementation scope.
- Frozen seven-source comparison: `docs/spikes/claremont-weather/comparison.html` (self-contained/offline) and its README. Actual NDFD regional fallback was 5.079 km, HRRR 3 km, DWD ICON-global 0.25°. Wind images share 21:00 UTC valid time; radar ~18:40–18:42 UTC on 2026-09-06. All viewing uses embedded/local images with zero provider requests.
- Expanded architecture and source research: `docs/research/everything-map.md`. One map owns viewport policy; integration caches aircraft snapshots, radar metadata/tiles, and wind fields; frontend consumes existing HA locations and animates wind locally.
- Approved delivery: one HACS Integration package bundling the card, automatic module loading, GitHub Actions/HACS/hassfest checks, and HACS install/upgrade acceptance. HACS packaging starts in slice 1 bootstrap.
- First-run setup is complete: repository creation/connection/initial push, Codex repository/hook trust and local scaffolding verified; `docs/FIRST_RUN.md` removed. Toolchains are pinned to Node 24.20.0/Python 3.14.2; CI uses uv 0.12.8. See `docs/development.md` for setup and temporary host paths.
- Slice 1 establishes v1 schemas, shared contract fixtures and offline stubs. Runtime frontend validators are generated by `make setup`/build; generated JS and release artifacts are ignored. Provider results exclude per-viewer envelope metadata. Development version `0.1.0-dev.1` now includes the people map/editor but not the complete four-layer product.
- Slice 2 establishes real HA setup/options, diagnostics, shared scheduler/cache and automatic module loading. Background collection is aircraft-only; weather needs viewer demand. Scheduler history/cooldowns survive reload; correcting configuration may release a permanent error without resetting pacing.
- Cache directory: `<HA config>/aviadilo_cache`; 512 MiB default disk budget counts full files, 8 MiB entry cap and 32 MiB retained blobs reserve transient headroom within 64 MiB. Atomic/cancellation-safe I/O, bounded metadata and recovery, separate validation/data times and provider-based radar retention are tested. Trusted config parent resolution runs off-loop; cache/entry symlinks are refused.
- HA 2026.9 replaces the native custom-element registry during startup. `static.py` serves a versioned bootstrap that waits for `home-assistant` before importing Lit/card code. Direct early module imports can become invisible to HA's final registry. Static routes persist for the HA process; extra-module membership is removed on unload.
- Backend tests now use real HA 2026.9.1 and pytest-asyncio 1.3.0, with offline DNS substitution. This host's sandbox blocks local asyncio socket wakeups; run checks with normal escalation when necessary. No project mypy exclusions were added. See `docs/development.md` for real-browser evidence and pending HACS acceptance.
- Slice 3 supplies authenticated WS/HTTP and the exported `AviadiloClient`/`createHaAdapter` API; external-layer card wiring remains pending. Initial status establishes HA-assigned subscription ID; client controls reconnection with `resubscribe:false`, latest selection/revision and visibility/disposal cleanup. Terminal status messages `aviadilo:closed` / `aviadilo:lease_expired` trigger recovery. Subscription registry removal is deferred/identity-guarded to avoid mutating HA's disconnect drain.
- Adapter hooks: capture publication context before async work with `service.capture(...)`, then `publish(...)`; register trusted radar `TileSource` resolvers with `service.tiles.register(...)`. Do not bypass shared scheduler/cache or relay arbitrary URLs. Tiles require current advertised frame, user/viewer/revision and viewport match. Initial caps: 4 WS subscriptions/connection, 16/user, 128 total; 8 HTTP requests/user, 32 total; 2 MiB PNG; 120s backend/150s frontend deadlines; no redirects. Full details are in development guide.
- HACS repository description/topics remain pending on GitHub; issue tracking is enabled. The session has SSH Git access but no GitHub API credential or `gh`. Local checks do not claim remote HACS/hassfest or HA installation acceptance.
- Slice 4 provides Leaflet map/viewport, the schema-driven graphical editor and device_tracker people layer. Keep the single JS bundle with inline Leaflet CSS: static.py only routes that file. Native controls avoid private HA selectors; all supported settings are editable, while aircraft/radar/wind rendering stays in later slices.
- Preview compatibility: HA 20260826.6 sets `.preview` through `hui-card`, but raw `hui-card-picker` previews omit it. `src/map/ha-preview.ts` detects composed picker ancestry before side effects; never persist preview context into saved configuration. All Aviadilo preview data/tiles are synthetic and offline. Development harness injects `fixtureHass`; previews do not read real HA locations.
- OSM runtime basemap policy: visible tiles only, one image/second and one in flight per page across cards, browser HTTP caching/referrer/attribution, no retries/prefetch. This is not backend/cross-device pacing. Mock OSM in automated pan/zoom checks. The current card reads integration info once per connection/configuration, with no external-layer demand; reload after changing integration anchor until later integration wiring.
- People freshness uses explicit position time when available, otherwise labelled entity-update time with GPS age unknown; never last_changed. Radius filtering precedes draw/count/fit and fails closed on missing anchors. Only central viewport fits; user interactions suspend fitting, popup/focus auto-pan is disabled, and reattachment aborts prior DOM listeners.
- Slice 5 installs the selected aircraft adapter at service startup. All viewers share one queued collector; no viewer/background demand means no collection or retained replay. Provider responses use bounded normalization and exact metre-radius clipping after outward integer-nmi requests. Both sources returned no-store on 2026-09-07: do not persist those responses or reset position ages on replay/revalidation.
- Aircraft component API: `src/layers/aircraft/index.ts` exports `AircraftController`, `AircraftLayer`, formatting/types and registers `aviadilo-aircraft-list`. Controller update/status/configure/select/subscribe drives both map and list; layer fitPoints returns fresh candidates only. Bounded selected trails, keyed updates, linked attribution and timer/disposal behavior are verified. `src/editor/aircraft-panel.ts` supplies optional reusable controls. The current card bundle does not import them yet; composition is slice 8. See development guide for API details.
- Temporary host state was lost before slice 5: Node24.20.0/Python3.14.2 were restored at the documented /tmp paths and existing .venv repaired. Earlier isolated HA configs, tokens and evidence paths no longer exist. Slice 5 reproduction artifacts are under `/tmp/aviadilo-slice5`, with no real household coordinates or credentials. Routine provider tests are offline; any future live check must use an explicitly public/synthetic area or have authorization for the household location.
- Slice 6 registers RainViewer/MRMS/KSOX metadata producers and trusted tile sources at startup. Metadata cadence (300s/120s) survives rapid hide/reopen; NOAA metadata and tiles share the same bucket. Preserve exact advertised NOAA times and RainViewer opaque paths. Empty timelines are unavailable; newest frame age over 15 minutes is stale. NOAA metadata generation field means receipt time, not observation time.
- Radar component APIs live in `src/layers/radar/README.md`: controller plus noninteractive canvas layer, timeline/legend and editor helpers. Buffer at most 3×32 256px tiles (24 MiB), canvas at most 8 MiB. RainViewer native z7; NOAA display sampling caps MRMS z7/KSOX z9 are deliberate conservative choices, not WMS native zooms. Complete RainViewer rain palette and exact NOAA legend are bundled; no remote legend fetch. Paused frame survives refresh; Latest follows newest. Full card wiring remains slice 8.
- `AviadiloClient.loadTile(tile, signal?)` supports per-caller cancellation without aborting shared waiters, including synchronous abort/replacement races. Source selection still belongs to the composing card. Forward current validated manifests/status and visibility to radar components; controller disposal and layer disposal have separate ownership. Live radar checks used one global overview and metadata request per provider; household coverage/full HACS/kiosk acceptance is still pending.
